<div class="container">
  <%= render "shared/notice" %>
  <div class="card border-0 shadow shadow-sm">
    <div class="card-header border-0 d-flex justify-content-between">
      <span class="fs-4">Visualizar Servico</span>
    </div>
    <div class="card-body">
      <div id="<%= dom_id @servico %>">
          <p>
            <strong>Descricao:</strong>
            <%= @servico.descricao %>
          </p>
          <p>
            <strong>Valor:</strong>
            <%= @servico.valor %>
          </p>
      </div>
    </div>
    <div class="card-footer border-0 d-flex justify-content-between">
      <div>
        <%= link_to edit_servico_path(@servico), class: "btn btn-primary" do %>
        <i class="bi bi-pencil-square"></i> Editar
        <% end %>
        <%= link_to "Voltar", servicos_path, class: "btn btn-secondary" %>
      </div>
      <%= button_to "Deletar", @servico, method: :delete, class: "btn btn-outline-danger border-0" %>
    </div>
  </div>
</div>
